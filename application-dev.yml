server:
  port: 8002

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    username: ding
    password: dyw20020304
    url: jdbc:mysql://124.222.35.20:3319/springcloud?useSSL=false&characterEncoding=utf-8&userUnicode=true
    druid:
      initial-size: 5       # 初始线程数
      max-active: 20        # 最大线程数
      max-wait: 60000       # 最大等待时间
      time-between-eviction-runs-millis: 60000      # 最大空闲实践
      min-idle: 5
eureka:
  client:
    #表示是否将自己注册进EurekaServer 默认为true
    register-with-eureka: true
    #是否从EurekaServer抓取已有的注册信息,默认为true.单节点无所谓,集群必须设置为true才能配合ribbon使用负载均衡
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
  instance:
    instance-id: payment8002
    prefer-ip-address: true
    # Eureka客户端向服务端发送心跳的时间间隔,单位为秒(默认为30秒)
    lease-renewal-interval-in-seconds: 3
    #Eureka服务端在收到最后一次心跳后等待时间上限,单位为秒(默认是90秒),超时将剔除服务
    lease-expiration-duration-in-seconds: 10


mybatis-plus:
  mapper-locations:
    - classpath:mapper/*.xml
  type-aliases-package: com.dyw.springcloud.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl